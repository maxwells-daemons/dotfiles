# ~/.zshenv: common settings to all zsh sessions

# All other zsh dotfiles are in ~/.config/zsh
export ZDOTDIR="$HOME/.config/zsh"

# Move .zsh_history to ~/.local/state/zsh/history
export HISTFILE="$HOME/.local/state/zsh/history"
